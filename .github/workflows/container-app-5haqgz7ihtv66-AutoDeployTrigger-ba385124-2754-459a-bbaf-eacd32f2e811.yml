name: Trigger auto deployment for container-app-5haqgz7ihtv66

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '//**'
    - '.github/workflows/container-app-5haqgz7ihtv66-AutoDeployTrigger-ba385124-2754-459a-bbaf-eacd32f2e811.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPP5HAQGZ7IHTV66_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPP5HAQGZ7IHTV66_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPP5HAQGZ7IHTV66_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v1
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistry5haqgz7ihtv66.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPP5HAQGZ7IHTV66_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPP5HAQGZ7IHTV66_REGISTRY_PASSWORD }}
          containerAppName: container-app-5haqgz7ihtv66
          resourceGroup: githubactiontestappspringaa0b
          imageToBuild: containerregistry5haqgz7ihtv66.azurecr.io/container-app-5haqgz7ihtv66:${{ github.sha }}
          dockerfilePath: Dockerfile


